package com.f4.mypet.ui.screens.procedure.createUpdate

enum class FrequencyOptions(val period: String) {
    Never("никогда"),
    Hours("указать в часах"),
    Days("указать в днях"),
    Weeks("указать в неделях")
}
